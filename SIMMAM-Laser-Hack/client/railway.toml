# Railway — Frontend Service (Static)
# Place this file in SIMMAM-Laser-Hack/client/

[build]
builder = "NIXPACKS"
buildCommand = "npm ci && npm run build"

[deploy]
startCommand = "npx serve dist -l $PORT"
